<% if current_user %>
  <div>
    <%= link_to toggle_favorite_product_path(product), remote: true, method: :post, id: "form", "data-id": "#{product.id}" do %>
      <%= render 'pages/like_button', product: product %>
    <% end %>
  </div>
<% end %>
