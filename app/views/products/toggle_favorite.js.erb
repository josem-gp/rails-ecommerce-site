parentDiv = document
  .querySelector('[data-id="<%= @product.id %>"]')
  .closest(".featured-card");
replaceIcon = document.querySelector('[data-id="<%= @product.id %>"]')
  .firstElementChild.firstChild;
if (window.location.href.match("/users/")) {
  if (replaceIcon.classList.contains("gray-heart")) {
  } else {
    parentDiv.remove();
  }
} else {
  if (replaceIcon.classList.contains("gray-heart")) {
    replaceIcon.classList.remove("gray-heart");
    replaceIcon.classList.add("red-heart");
  } else {
    replaceIcon.classList.remove("red-heart");
    replaceIcon.classList.add("gray-heart");
  }
}
